#include <bits/stdc++.h>
#define mx 100007
#define ld long double 
using namespace std;

set<int>st[mx];
map<int>pl[mx], win[mx];

int main()
{
	int n,q;
	scanf("%d %d", &n, &q);

	while(q--)
	{
		int tp;
		scanf("%d", &tp);

		if(tp==1)
		{
			int x,y;
			scanf("%d %d", &x, &y);


		}
		else if(tp==2)
		{

		}
		else
		{

		}
	}
}