#include <iostream>

using namespace std;

int main()
{
    int num=1;
    int NUM=0;
    int n;
    cin>>n;
    int m[n];
    for(int i=0;i<;i++)
        cin>>m[i];
    for(int i=0;i<(n-1);i++)
    {
        if(m[i]<=m[i+1])
            num++;
        else

    }
    return 0;
}
