#include <bits/stdc++.h>
using namespace std;

vector<int> arr(110);

int main()
{
    int n;
    cin>>n;

    for(int i=0;i<n;i++)
        cin>>arr[i];

    int cnt= 0;

    while( !is_sorted( arr.begin(), arr.end() ) )
    {

    }
}
