#include <bits/stdc++.h>
#define ll long long
#define pll pair<ll,ll>
using namespace std;

ll arr[100005];

int main()
{
    ll n;
    cin>>n;

    for(int i=0;i<n;i++)
        cin>>arr[i];

    pll cmp[n];
    pll later[n];
    for(int i=0;i<n;i++)
    {
        cin>>cmp[i].first;
        cmp[i].second=i;
        later[i]=cmp[i];
    }

    sort(cmp, cmp+n);
    sort(arr, arr+n);

    for(int i=0;i<n;i++)
        cout<<arr[  ]

}
