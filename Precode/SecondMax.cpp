#include <bits/stdc++.h>
using namespace std;
int mx=-1;
int mx1=10000;
int secondMax(int i, int n, int *a)
{
    if(i==n)
        return mx1;
    if(a[i]>mx)
        secondMax()
}
int main()
{
    int n;
    cin>>n;
    int arr[n];
    int SecMax=
}
