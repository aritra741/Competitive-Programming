#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    while(getline(cin,s))
    {
       int len=s.length();
       for(int i=0;i<len;i++)
        if( (s[i]<65) || (s[i]>90 && s[i]<97) || s[i]>122 )
                    printf("%c", s[i]);
        else
       {
           (
            t[0]=='a' || t[0]=='i' || t[0]=='o' || t[0]=='e' || t[0]=='u' || t[0]=='A' || t[0]=='E' || t[0]=='I' || t[0]=='O' || t[0]=='U')
            {
                cout<<t<<"ay ";
            }
            else
            {
                int len=t.length();
                for(int i=1;i<len;i++)
                    cout<<t[i];
                cout<<t[0]<<"ay ";
            }
        }
    }
}
