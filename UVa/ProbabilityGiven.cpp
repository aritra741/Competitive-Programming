#include <bits/stdc++.h>
using namespace std;
int main()
{
    while(1)
    {
        int n,r;
        cin>>n>>r;

        double arr[n];

        for(int i=0;i<n;i++)
            cin>>arr[i];

        for(int i=0;i<n;i++)
        {
            double d=
        }
    }
}
