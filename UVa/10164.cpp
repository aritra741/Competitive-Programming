#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n;

    while(cin>>n and n)
    {
        n= 1<<n;
        n--;

        int arr[n];

        for( int i=0;i<n;i++ )
            cin>>arr[i];

        for( int mask=0;i<<mask;m++ )
        {
            for( int i=0;i<n;i++ )
            {
                if(  )
            }
        }
    }
}
